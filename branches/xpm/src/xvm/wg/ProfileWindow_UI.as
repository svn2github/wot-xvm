package 
{
    import net.wg.gui.lobby.window.*;
    
    public dynamic class ProfileWindow_UI extends net.wg.gui.lobby.window.ProfileWindow
    {
        public function ProfileWindow_UI()
        {
            super();
            return;
        }
    }
}
