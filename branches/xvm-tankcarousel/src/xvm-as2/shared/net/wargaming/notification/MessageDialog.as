intrinsic class net.wargaming.notification.MessageDialog extends net.wargaming.Dialog
{
    var title, message, messageField, wndBg, cancelBtn, submitBtn, submitLabel, cancelLabel;
    static function show(dialog, focusSubmit, sumbitVisible, store);
    static var messageDialogSource = "MessageDialog";
}
