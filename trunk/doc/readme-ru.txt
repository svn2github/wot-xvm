Этот мод является преемником следующих модов:
  * OverTargetMarkers
  * Отключение посмертной панели
  * Управление зеркалированием иконок
  * Управление "ушами" (ширина и прозрачность)
  * Часы на экране загрузки боя
  * Рейтинг игроков в игре (только в пакете xvm-full)

Сайт проекта: http://code.google.com/p/wot-xvm/
Поддержка:    http://forum.bulychev.net/viewforum.php?f=6

Всем, кто считает что мод грузит сервер статистики, сюда:
http://bulychev.net/blog/2012/02/pozitsiya-wg-v-otnoshenii-modov-so-statisti/

Конфигурационный файл мода:
  \res_mods\<game version>\gui\flash\XVM.xvmconf (основной конфиг)
  \res_mods\<game version>\gui\flash\OTMData.xml (в режиме совместимости)
Можно выбрать готовый конфиг из папки \xvm-doc\samples\
Можно создрать новый конфиг или изменить существующий с помощью онлайн-редактора:
  http://bulychev.net/generator/

Параметры конфига XVM.xvmconf:

battle
  mirroredVehicleIcons:   false - отключить зеркалирование иконок танков (полезно
                                  для альтернативных иконок)
  showPostmortemTips:     false - отключить всплывающую внизу панель после смерти
  drawGrid:               true  - включить отображение сетки координат (может быть
                                  полезным для выравнивания элементов маркера)

rating
  showPlayersStatistics:  true  - включить отображение статистики. Глобальный
                                  переключатель - без него модуль статистики будет
                                  везде отключен.
  loadEnemyStatsInFogOfWar: true - включить загрузку данных по противникам в
                                  "тумане войны" (experimental)

battleLoading
  showClock:               false - выключить отображение часов на экране загрузки боя
  showChances:             true - включить отображение шансов на победу
                                  Обсуждение: http://forum.bulychev.net/viewtopic.php?f=4&t=26
  formatLeft:              "text" - формат отображения для левой панели (допускаются
                                  макроподстановки статистики - см.ниже)
  formatRight:             "text" - формат отображения для правой панели

statisticForm
  formatLeft:              "text" - формат отображения для левой панели (допускаются
                                  макроподстановки статистики - см.ниже)
  formatRight:             "text" - формат отображения для правой панели

playersPanel
  alpha:                   0..100 - задает прозрачность "ушей". 0 - прозрачные, 100 - не прозрачные
  iconAlpha:               0..100 - задает прозрачность иконок в "ушах". 0 - прозрачные, 100 - не прозрачные
  medium                   режим medium1
    width:                 0..250 - задает ширину поля имени игрока. По умолчанию: 46
    formatLeft:            формат отображения
    formatRight:           формат отображения
  medium2                  режим medium2
    width:                 0..250 - задает ширину поля имени игрока. По умолчанию: 65
    formatLeft:            формат отображения
    formatRight:           формат отображения
  large:                   режим large
    width:                 0..250 - задает ширину поля имени игрока. По умолчанию: 170
    nickFormatLeft:        формат отображения ника
    nickFormatRight:       формат отображения ника
    vehicleFormatLeft:     формат отображения названия танка
    vehicleFormatRight:    формат отображения названия танка

colors
  eff: [ ]              значения для динамического цвета по эффективности
  rating: [ ]           значения для динамического цвета по общему проценту побед
  kb: [ ]               значения для динамического цвета по кило-боям
  hp: [ ]               значения для динамического цвета по количеству здоровья (только в маркерах)
  hp_ratio: [ ]         значения для динамического цвета по проценту здоровья (только в маркерах)

alpha
  eff: [ ]              значения для динамической прозрачности по эффективности
  rating: [ ]           значения для динамической прозрачности по общему проценту побед
  kb: [ ]               значения для динамической прозрачности по кило-боям
  hp: [ ]               значения для динамической прозрачности по количеству здоровья (только в маркерах)
  hp_ratio: [ ]         значения для динамической прозрачности по проценту здоровья (только в маркерах)

iconset			наборы иконок для:
  battleLoading:          экрана загрузки боя
  statisticForm:          окне статистики боя (по клавише Tab)
  playersPanel:           ушей
  vehicleMarker:          маркеров над танками
путь к стандартным иконкам: "../maps/icons/vehicle/contour" (используется по умолчанию)
пути допускаются только относительные

players: [ ]            массив данных по игрокам и кланам:
пример для игрока: { "nick": "sirmax2", "icon": "../../../clanicons/XVM.png", "x": 0, "y": 4, "w": 16, "h": 16, "alpha": 100 },
пример для клана:  { "clan": "[WG]",    "icon": "../../../clanicons/WG.png",  "x": 0, "y": 4, "w": 16, "h": 16, "alpha": 100 },
где:
  nick  - ник игрока (допускаются только полные ники)
  clan  - название клана. Поиск осуществляется по подстрокам. Например:
          clan: "[RED]" - ищется клан с полным названием "RED"
          clan: "[RED"  - ищутся все кланы, которые начинаются с "RED"
          clan: "RED"   - ищутся все кланы, которые содержат "RED"
  icon  - имя файла с иконкой клана (допускается использовать относительные пути)
  x,y   - положение иконки относительно иконки танка
  w,h   - ширина и высота иконки
  alpha - прозрачность иконки (0..100, динамическая прозрачность не будет работать)

markers
  ally
    alive
      normal
        vehicleIcon:    настройки для текущего состояния значка типа танка
        healthBar:      настройки для текущего состояния строки здоровья
        damageText:     настройки для текущего состояния отлетающего урона
        contourIcon:    настройки для текущего состояния иконки танка
        levelIcon:      настройки для текущего состояния уровня танка
        actionMarker:   настройки для текущего состояния маркера атаки/помощи
        textFields: [ ] настройки для текущего состояния текстовых полей
      extended: ...
    dead
      normal: ...
      extended: ...
  enemy
    alive
      normal: ...
      extended: ...
    dead
      normal: ...
      extended: ...


(Устарело, рекомендуется использовать новый конфиг XVM.xvmconf)
Возможности, недоступные из редактора OTM (OTMData.xml):

- если атрибут color у какого-то тэга отсутствует, мод использует цвет клиента
  по умолчанию для этого типа маркера (в т.ч. отдельные цвета для взводных / ТК);

- атрибут lcolor тега fill задаёт цветовой переход для полоски здоровья;

- все цвета, за исключением фильтра тени и рамки полосы здоровья могут быть заменены на
  динамические с помощью макроподстановок (см. ниже);

- тэг rating/showPlayersStatistics позволяет включить отображение статистики в infoText (см. ниже)
  <rating>
    <showPlayersStatistics>true</showPlayersStatistics>
  </rating>

- тэг combatScrollText/hitKind позволяет переключить отображение всплывающих повреждений
  в режим показа процентов
  <combatScrollText>
    <hitKind>Relative</hitKind>

- тэг combatScrollText/font позволяет задать свой шрифт для вылетающего дамага

- атрибуты healthRatio/format/@font, playerName/format/@font, vehicleName/format/@font,
  currentHealth/format/@font позволяют задать свой шрифт для соответствующих строк

- тег components/*/infoText задаёт формат текстовых строк по умолчанию (аналогично
  components/*/currentHealth), тэг behavior/*/*/*/infoText задаёт положение и содержимое
  текстовых строк.

Используемые макросы:
  В ушах:
    {{nick}}       - ник игрока
    {{vehicle}}    - название танка

  В маркерах над танком:
    {{hp}}         - текущее здоровье
    {{hp-ratio}}   - процент текущего здоровья (без символа '%')
    {{hp-max}}     - максимальное здоровье
    {{nick}}       - ник игрока
    {{vehicle}}    - название танка
    {{level}}      - уровень танка арабскими цифрами
    {{dmg}}        - количество отнятого здоровья
    {{dmg-ratio}}  - процент отнятого здоровья (без символа '%')

  Макроподстановки статистики (необходимо включить rating/showPlayersStatistics):
    {{rating}}     - процент побед
    {{eff}}        - эффективность игрока (http://wot-news.com/index.php/stat/calc/ru/)
    {{kb}}         - количество кило-боев (округление количества боев до 1000).
    {{battles}}    - количество боев
    {{wins}}       - количество побед
    {{rating:3}}   - процент побед с выравниванием слева до трех символов
    {{eff:4}}      - эффективность игрока с выравниванием слева до четырех символов
    {{kb:3}}       - количество кило-боев с выравниванием слева до трех символов

  Макросы динамического цвета:
    {{c:hp}}       - цвет в зависимости от текущего здоровья (только в маркерах)
    {{c:hp-ratio}} - цвет в зависимости от процента текущего здоровья (только в маркерах)
    {{c:eff}}      - цвет в зависимости от эффективности
    {{c:rating}}   - цвет в зависимости от процента побед
    {{c:kb}}       - цвет в зависимости от количества кило-боев

  Макросы динамической прозрачности:
    {{a:hp}}       - прозрачность в зависимости от текущего здоровья (только в маркерах)
    {{a:hp-ratio}} - прозрачность в зависимости от процента текущего здоровья (только в маркерах)
    {{a:eff}}      - прозрачность в зависимости от эффективности
    {{a:rating}}   - прозрачность в зависимости от процента побед
    {{a:kb}}       - прозрачность в зависимости от количества кило-боев

Примеры поля "format":

1. Отобразить количество кило-боев, эффективность и процент побед без изменения цвета:
  "{{kb}} {{eff}} {{rating}}"
2. То же самое, но раскрасить каждое число в зависимости от его значения:
  "<font color='{{c:kb}}'>{{kb}}</font> <font color='{{c:eff}}'>{{eff}}</font> <font color='{{c:rating}}'>{{rating}}</font>"
3. То же, что и 2, но с выравниванием колонок:
  "<font face='Consolas' size='11'><font color='{{c:kb}}'>{{kb:3}}</font> <font color='{{c:eff}}'>{{eff:4}}</font> <font color='{{c:rating}}'>{{rating:3}}</font></font>"
4. Отобразить процент побед, подкрашенный в зависимости от рейтинга эффективности:
  "<font color='{{c:eff}}'>{{rating}}</font>"

Примеры использования динамического цвета и прозрачности:
В XVM.xvmconf:
  "color": "{{c:eff}}"
В OTMData.xml:
  <infoText x="0" y="-50" color="{{c:eff}}" alpha="75" visible="true" format="{{kb}} {{eff}} {{rating}}" bold="true"/>
  <currentHealth x="-41" y="-38" color="{{c:hp}}" alpha="{{a:hp}}" visible="true"/>
