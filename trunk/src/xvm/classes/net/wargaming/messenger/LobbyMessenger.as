intrinsic class net.wargaming.messenger.LobbyMessenger extends gfx.core.UIComponent
{
    function handleToggleContacsWindow(event);
    var propertiesManager;
    var windowManager;
    function onRecieveChannelMessage(cid, message, blinking);
    function handleConfirmChannelPageFormComplete(event);
    function onSetJoinedToLazyChannel(cid, joined);
    function onConnectToSecureChannel(channelId, channelInfo);
    function onUserInfoReceived();
}