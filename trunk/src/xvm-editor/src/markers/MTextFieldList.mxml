<?xml version="1.0" encoding="utf-8"?>
<c:PanelComponent
	xmlns:fx="http://ns.adobe.com/mxml/2009"
	xmlns:s="library://ns.adobe.com/flex/spark"
	xmlns:mx="library://ns.adobe.com/flex/mx"
	xmlns:c="components.*"
	xmlns:m="markers.*"
	title="Текстовые поля" width="100%" height="80">
	<fx:Script>
		<![CDATA[
			import mx.collections.ArrayList;

			import spark.events.IndexChangeEvent;
			
			public var textFields:ArrayList = new ArrayList();

			protected function button1_clickHandler(event:MouseEvent):void
			{
				if (fields.selectedIndex >= 0)
					fields.dataProvider.removeItemAt(fields.selectedIndex);
			}

			protected function button2_clickHandler(event:MouseEvent):void
			{
				// TODO Auto-generated method stub
				fields.dataProvider.addItem({ label: "Текст" });
			}

			protected function list1_changeHandler(event:IndexChangeEvent):void
			{
				//var item:* = list.dataProvider.getItemAt(evt.newIndex);
				//if (item.type != "employee") {
				//	evt.preventDefault();
				//}
			}

		]]>
	</fx:Script>
	<mx:Image right="5" y="-18" width="16" height="16" source="@Embed(source='images/cancel.png')" toolTip="Вернуть настройки по умолчанию" useHandCursor="true" buttonMode="true"/>
	<s:HGroup left="10" right="10" top="5" bottom="5" gap="2" verticalAlign="middle">
		<s:List width="100%" height="100%" id="fields" change="list1_changeHandler(event)" >
			<s:dataProvider>
				<s:ArrayList>
				</s:ArrayList>
			</s:dataProvider>
		</s:List>
		<mx:Image width="16" height="16" id="del" source="@Embed(source='images/delete.png')" toolTip="Удалить" useHandCursor="true" buttonMode="true"/>
		<mx:Image width="16" height="16" id="add" source="@Embed(source='images/add.png')" toolTip="Вставить" useHandCursor="true" buttonMode="true"/>
	</s:HGroup>
</c:PanelComponent>
